import { CreateMenuRequest } from './create-menu-request.dto';

/**
 * Used when updating an existing menu.
 * Currently shares the same shape as CreateMenuRequest.
 */
export type UpdateMenuRequest = CreateMenuRequest;
